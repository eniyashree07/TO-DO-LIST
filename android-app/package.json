{
  "name": "lavender-planner-android",
  "version": "1.0.0",
  "description": "Lavender Planner - Android app (Capacitor wrapper for the To-Do / Activity Planner)",
  "private": true,
  "scripts": {
    "sync:web": "powershell -Command \"Copy-Item ..\\index.html,..\\styles.css,..\\app.js -Destination www -Force\"",
    "sync": "npm run sync:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
